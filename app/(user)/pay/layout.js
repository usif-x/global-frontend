export const metadata = {
  title: "Payment Status",
  description: "Confirm the status of your payment",
  robots: "noindex, nofollow",
};

export default function PayLayout({ children }) {
  return children;
}
